# Port the server listens on. Railway sets this automatically; only needed locally.
PORT=3000

# Bind host. 0.0.0.0 is required on Railway / inside containers.
HOST=0.0.0.0

# Set to "production" on Railway for combined access logs and asset caching.
NODE_ENV=production
